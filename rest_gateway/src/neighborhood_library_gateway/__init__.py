"""Neighborhood Library REST gateway package (FastAPI).

Application code lives in :mod:`neighborhood_library_gateway.app`. Public HTTP surface is documented
via FastAPI/OpenAPI at runtime. For narrative docs see the repository ``README`` and ``docs/``.

Future: versioned subpackages (e.g. ``v1`` routes), dependency-injected gRPC channels for testing.
"""
